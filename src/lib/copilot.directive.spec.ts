import { CopilotDirective } from './copilot.directive';

describe('CopilotDirective', () => {
  it('should create an instance', () => {
    const directive = new CopilotDirective();
    expect(directive).toBeTruthy();
  });
});
